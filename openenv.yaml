name: pii-redaction-env
version: 1.0.0
description: GDPR/HIPAA compliance environment for PII redaction in unstructured text
author: Your Name
tags: [compliance, privacy, nlp, real-world]

entrypoint: src.environment:RedactionEnvironment

observation_space:
  type: RedactionObservation
  schema: src.models:RedactionObservation

action_space:
  type: RedactionAction
  schema: src.models:RedactionAction

reward_range: [-10.0, 5.0]

tasks:
  - gdpr_contract_easy
  - hipaa_medical_medium
  - security_logs_hard

docker:
  port: 7860
  cmd: python scripts/baseline.py
